# しごとボード 使い方ガイド

## 1日の流れ

| 時間 | やること | 使うタブ |
|---|---|---|
| 出社直後 | 発車標で今日の期限（遅延・まもなく）を確認。到着案内で「催促」ボタンが点灯していたらワンタップ → 催促文の下書きが自動生成 | 運行 → 文書 |
| 会議の5分前 | 該当する過去の会議を開いて「予習ブリーフ」を生成。前回の決定事項・自分の宿題・積み残し論点を1枚で確認 | 記録 |
| 会議の直後 | メモを貼り付けて「構造化」。出発前点検（送信プレビュー）で確認して登録すると、ToDoは発車標へ、依頼は到着案内へ、用語・人物候補はノートへ自動配置 | 取込 |
| 日中随時 | 終わったタスクにチェック（発車）。読んだ資料・ニュースは「資料・ニュース」モードで要約収蔵。分からない用語はノートで検索 | 運行・取込・ノート |
| 退勤前 | 日誌に一言記帳（上司・先輩に何か言われた日は「指摘を含む」にチェック）。報告が必要なら文書タブで「報告」→ 完了ToDoにチェック → 生成 → コピーしてメールへ | 日誌 → 文書 |

## 1週間の流れ

| タイミング | やること |
|---|---|
| 月〜木 | 上記の1日の流れを回すだけ。入力は「会議メモ貼り付け」と「日誌一言」の2つだけで、あとは自動で溜まる |
| 金曜夕方 | 日誌タブで「直近1週間の振り返り」を生成。今週の動き・完了・指摘・積み残しが文章化されるので、週報のたたき台に |
| 金曜夕方 | 設定タブで「JSONに書き出す」→ バックアップ保管（PC入替・ブラウザ初期化対策） |
| 月1回・評価期 | 日誌タブで期間を指定して「面談用サマリ」を生成。1on1・評価面談の自己アピール材料が完成 |
| 随時 | ミスしたら文書タブ「謝罪」、判断に迷ったら「相談」、昔の話を思い出したいときは記録タブで検索 or 「質問」 |

## 端末別の開き方

公開URL（GitHub Pages 有効化後）: **https://kanipan0611.github.io/Job-assistant/**

| 端末 | 開き方 |
|---|---|
| スマホ（私物） | 公開URLをブラウザで開く → iPhone: Safariの共有メニュー →「ホーム画面に追加」／ Android: Chromeメニュー →「アプリをインストール」。以後アプリとして起動（オフライン起動対応） |
| 自分のPC | 公開URLを Chrome/Edge で開く → アドレスバー右端のインストールアイコンでアプリ化。または `release/shigoto-board.html` をダウンロードして開く |
| 社内PC | 基本は `release/shigoto-board.html` を1ファイルダウンロードしてダブルクリック（インストール不要・管理者権限不要）。社内ネットワークから公開URLが開けるならPWAインストールも可 |

### 注意

- **端末間でデータは同期されません**（各端末のブラウザ内にのみ保存）。端末をまたぐときは 設定 → JSON書き出し → 移行先で読み込み。
- AI機能（構造化・振り返り・下書き生成など）は `api.anthropic.com` への通信が必要。社内ネットワークで遮断されている場合、AI機能は使えません（タスク管理・日誌・検索はオフラインで動作）。
- APIキーは端末ごとに設定タブで登録します。

## 漏洩対策の使い分け

| モード | 何が起きるか | こういう時に |
|---|---|---|
| 完全オフラインモード ON | 外部送信ゼロ。AI機能は停止、それ以外は全部使える | 会社のルールが未確認・厳しい環境 |
| 通常（既定） | 送信前に自動マスキング（人名・会社名・金額・連絡先・NGワード・人物ノートの名前）→「出発前点検」で全文プレビュー → 承認した場合のみ送信 | AIをフル活用したい時 |

NGワード辞書（設定タブ）に顧客名・案件名を登録しておくと、その語は必ず伏せ字になります。
